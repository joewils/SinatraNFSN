<h2>Dashboard</h2>
<p>Sinatra version: <%= Sinatra::VERSION %></p>
<p>This is a proof of concept for hosting a Ruby/Sinatra app with SQLite on NearlyFreeSpeech.net.</p>

<h3>Database Tables</h3>
<table border="1" cellpadding="5">
  <tr>
    <th>Table</th>
    <th>Records</th>
  </tr>
  <% @counts.each do |table, count| %>
  <tr>
    <td><a href="/<%= table %>"><%= table %></a></td>
    <td><%= count %></td>
  </tr>
  <% end %>
</table>
